dist: bionic

services:
    - docker

before_install:
    - docker pull python:latest
    - docker run -itd --name mypython python:latest
    - docker ps
    - docker exec -it mypython /bin/bash "cd /etc" 

script:
    python second.py
